"""DSPy/GEPA adapters for search-slice students (one step each).

Registers ``search-search`` and ``search-suggest`` optimize modules with
dataset loaders (HF for search, local Opik I/O export for suggest), GEPA
metric adapters, and student programs used by ``optimize.main``.
"""
